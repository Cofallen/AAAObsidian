---
score: 0
tags: daily
created: 2026-07-14 11:14:19
---
🔙 [[MyLife/Yearly/2026|年度展望]] | 📂 [[MyLife/Monthly/2026-07|本月月记]] | 📅 [[MyLife/Weekly/2026-W29|本周周记]] | 📝[[总览知识]]
```dataviewjs
// ===== 日期 =====
const today = moment(dv.current().file.name, "YYYY-MM-DD");
const yesterday = today.clone().subtract(1, "day");

// ===== 昨天页面 =====
const yPage = dv.page(yesterday.format("YYYY-MM-DD"));

// ===== 昨天 score（唯一依据）=====
let yScore = yPage?.score ?? null;

// ===== 状态判断（只看昨天）=====
let state = "start";

if (yScore !== null) {
    if (yScore >= 60) state = "good";
    else if (yScore >= 30) state = "mid";
    else state = "bad";
}

// ===== 读取语录文件 =====
const file = app.vault.getAbstractFileByPath("Templates/ZZZCompare.md");
const raw = await app.vault.read(file);

// ===== 解析函数 =====
function parseSection(name) {
    const match = raw.match(new RegExp(name + ":[\\s\\S]*?(?=\\n\\w+:|$)"));
    if (!match) return [];
    return match[0]
        .replace(name + ":", "")
        .split("\n")
        .map(l => l.replace(/^-\\s*/, "").trim())
        .filter(Boolean);
}

// ===== 映射（启动语 + 昨日评价）=====
const messages = {
    start: parseSection("start"),
    good: parseSection("better"),
    mid: parseSection("mid"),
    bad: parseSection("worse")
};

const pick = arr => arr[Math.floor(Math.random() * arr.length)];

const startMsg = pick(messages.start);
const stateMsg = pick(messages[state] || messages.mid);

// ===== 输出 =====
dv.paragraph(`📌 ${startMsg} ${stateMsg}`);
```
---

> [!review]+ 📖 艾宾浩斯复习
> ```dataview
> LIST rows.L.text
> FROM "MyLife/Daily"
> WHERE
> file.day = date(this.file.day)-dur(1 day)
> OR file.day = date(this.file.day)-dur(2 days)
> OR file.day = date(this.file.day)-dur(3 days)
> OR file.day = date(this.file.day)-dur(7 days)
> OR file.day = date(this.file.day)-dur(14 days)
> OR file.day = date(this.file.day)-dur(30 days)
> FLATTEN file.lists as L
> WHERE contains(meta(L.section).subpath, "今日知识整理")
> GROUP BY file.link
> ```


## 📅 每日计划
```dataviewjs
const tasks = dv.current().file.tasks;
const total = tasks.length;
const done = tasks.where(t => t.completed).length;

const score = total ? Math.round(done / total * 80) : 0;
const ratio = total ? done / total : 0;

// ===== 颜色 =====
let color = "#ff4d4f"; // 🔴
if (ratio >= 0.6) color = "#52c41a"; // 🟢
else if (ratio >= 0.3) color = "#faad14"; // 🟡

// ===== 读取外部文案文件 =====
const file = app.vault.getAbstractFileByPath("Templates/ZZZSays.md");
const raw = await app.vault.read(file);

// ===== 解析 low / mid / high =====
function parseSection(name) {
    const match = raw.match(new RegExp(name + ":[\\s\\S]*?(?=\\n\\w+:|$)"));
    if (!match) return [];
    return match[0]
        .replace(name + ":", "")
        .split("\n")
        .map(l => l.replace(/^-\\s*/, "").trim())
        .filter(Boolean);
}

const messages = {
    low: parseSection("low"),
    mid: parseSection("mid"),
    high: parseSection("high")
};

// ===== 选择等级 =====
let pool = messages.low;
if (ratio >= 0.6) pool = messages.high;
else if (ratio >= 0.3) pool = messages.mid;

// ===== 随机一句 =====
const msg = pool.length
    ? pool[Math.floor(Math.random() * pool.length)]
    : "";

// ===== 进度条 =====
const bar = `
<div style="font-family: monospace; font-size: 14px;">
  
  <div style="
    width: 100%;
    background: #f0f0f0;
    border-radius: 8px;
    overflow: hidden;
    height: 14px;
  ">
    <div style="
      width: ${ratio * 100}%;
      background: ${color};
      height: 14px;
      transition: width 0.3s ease;
    "></div>
  </div>

  <div style="margin-top:6px; color: #333;">
    █ ${done}/${total}  →  ${score} ｜ ${msg}
  </div>

</div>
`;

dv.paragraph(bar);
```
- [ ] 英语200词
- [ ] 武完结第一章
- [ ] 整理前面的知识
- [ ] 整理待归档

## 💡 灵感

- 

# ✍️ 今日知识整理  
  
- 

## 📝 总结与反思 

- 